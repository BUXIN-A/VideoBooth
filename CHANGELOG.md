# 更新日志

VideoBooth 的重要变更都记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-26

首个正式发布版本。

### 变更
- 版本号由 0.1.0 提升至 1.0.0（程序文件版本、产品版本与程序集标识同步更新）
- 启动画面展示时长由 3 秒缩短为 2 秒
- 拍照反馈由文字提示改为画面全屏白闪（约 150 ms 渐隐），不再遮挡画面内容；拍照失败时仍保留文字提示

### 文档
- README 补充当前版本号、启动时长与拍照反馈说明

## [0.1.0] - 2026-09-25

初始版本，提供完整的展台功能。

### 新增
- 全屏展台窗口：OpenGL 纹理渲染，画面按比例完整适配窗口，支持缩放、每次 90° 旋转与拖动（拖动边界自动收敛，保证画面完整可见）
- 左下角预览框：显示画面全景与当前可见区域的虚线框，可拖动虚线框平移画面
- 功能栏：选择 / 批注 / 橡皮 / 旋转 / 锁定 / 拍照 / 相册 / 设置 / 最小化 / 退出，可置于底部或两侧
- 批注：10 种颜色与粗细设置（1–30，默认 5）；橡皮：5 档大小与「全部清除」
- 「更多」面板：批注 / 橡皮模式下展开，点击面板、标签与功能栏以外的空白区域可收起
- 笔迹独立存取：实时画面使用 `_live.ann.png`，每张照片使用 `<照片名>.ann.png`，切换画面时自动保存与恢复，退出时随临时照片一并清理
- 拍照：WIC 编码 JPG（质量 85%），原画面与笔迹分离存放，相册中查看时笔迹仍可继续擦除
- 相册：缩略图列表（滚轮横向滚动 / 拖动滚动）、删除、单个另存为、全部导出、导入照片、全屏查看
- 导出照片时可选择是否「合成笔迹」（默认勾选）；删除照片时一并删除其笔迹文件
- 锁定画面：定格当前画面并停止拉流（保留设备句柄），锁定期间仍可批注与拍照
- 触摸手势：单指等同鼠标操作、双指捏合缩放、三指及以上平移；触摸与系统合成的鼠标消息自动去重
- 设置面板（在主窗口内绘制，全屏窗口下始终可见）：功能栏位置、临时文件夹、运行日志开关、默认摄像头、刷新率、分辨率、自动曝光、垂直同步、双缓冲、抗锯齿
- 启动流程：启动画面 → 单实例检测（已有实例时激活其窗口并退出本次启动）→ 配置加载与损坏修复 → 摄像头检测与选择
- 摄像头热插拔自动重连；无摄像头时显示占位图并提示
- 配置文件 `config.json` 与备份 `config.bak.json`，支持字段级校验、默认值合并与自动修复写回
- 运行日志可选写入 `VideoBooth.log`（默认关闭，关闭时不创建日志文件）

### 性能
- 帧数据、笔迹与各界面面板均按需上传纹理，笔迹仅上传脏矩形
- 渲染按需出帧（两次呈现间隔不小于 15 ms），最小化与设置面板无重绘时不出帧
- 锁定画面、查看照片、打开设置、最小化时暂停拉流；采集端按配置刷新率丢弃多余帧
- 拍照的整帧 JPG 编码在后台线程串行完成，界面不阻塞
- 相册仅解码缩略图（最长边 256），全屏展示时才加载原图，解码失败的条目不再重试
- 相册卡片按横坐标排列，命中判定与渲染均可提前退出，按钮图标在循环外一次取出
- 采集线程缓存协商后的分辨率，避免每帧查询媒体类型
- 绘制热路径优化：预览框与文字合成预先收敛绘制范围、归一化改用乘法；笔迹绘制跳过圆外像素的开方并预乘颜色分量
- WIC 工厂改为一次性初始化，消除界面线程与后台保存线程并发首次调用时的竞态
- 日志仅在错误级同步落盘，并以栈缓冲替代每次调用的堆分配
